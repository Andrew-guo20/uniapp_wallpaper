1.浏览器和服务器 对于URL 有长度限制
2.unicloud-db    数据库查询组件 collection（value为要查询的数据库名称，可联表查询 -- 多个表名拼接到一起，用逗号隔开，也可以定义数组 colList来代替 ）
const colList = ref([
	db.collection("demo-wallpaper").orderBy("createTime desc").getTemp(),
	db.collection("demo-classify").getTemp()
])
 v-slot:default 为查询到的数据data、error、loading、hanmore等   
同时也能用 field   

3.触底 生命周期 onReachButton
4.前后端分离  --  云函数/云对象 -- 理解为后端，可以提供接口（直接上传到云端执行）
在服务端 进行对数据的操作，不在客户端操作数据 
新建云函数/云对象 - uniClound - cloudfuncitions - 右键新建（勾选云对象）- index.name.js - 编写代码 - demoObj 右键 运行本地云函数/云对象 自动创建 同名.param.js
在页面中 const demoObj = uniCloud.importObject("demoObj")     导入，在调用 demoObj 的方法 如getUser
5.JQL方式 会受schema限制/传统方式 不会受schema限制   schema 理解为 对数据的限制
JQL 不支持 ①更新数组内指定下标的元素 ②更新数组内匹配条件的元素    等于说 得知道数据库要改修改的数据 copy一份 在进行修改
传统方式   --  数据库操作符  数组:dbCmd.push(['data1','data2'])
6.云函数：通用预处理器 -- _before （拦截器 有无token之类的）；后处理器 _after(error,result),有after之后一定要将结果return(是否超时)；
		定时运行 _timeing（自动执行）-- 要package.json中进行配置 -- 定时触发-云函数package.json -cloudfunction-config
  云对象的API：获取客户端信息 getClientInfo()
7.将云对象/云函数上传，部署到云端
8.uniCloud --
    url化（自主开发api接口 -- Dcloud首页 -- unicloud（服务空间） -- 云函数/云对象  --  函数/对象列表 -- 详情 -- 云函数URL化 -- 默认长域名/自己搞个） 
    默认域名/云对象或者云函数/函数名（实际调用的函数名）	
   利用 apipost 记录 每个项目 所调用的接口	
9.云存储：（内置存储/扩展存储）  	uni-file-picker 文件上传   --   组件 无法实现点击删除 同步删除
	客户端API（现在一般在客户端-前端 直传 ，用云函数API，还要经过一层服务端，效率低）
		uni.chooseImage 本地选择图片
 		uniCloud.uploadFile 实现本地图片上传（单张/批量）-- 公共权限（资源永久有效）/私有权限（资源有时间限制）-- cloudPath（上传资源名称）
		自定义上传文件路径名（利用dayjs实现 cloudPath = filename = dayjs().format("yyymmmdd"+"/"+Date.now()+".jpg")）
		及onUploadProcess上传进度（上传进度回调）
		getTEmpFileURL() 把文件的fileid转换成临时URL（https） --- 公共权限没必要用，用来会发起请求，也可以自己封装 cloud to https
		通过云对象调用deleteFile批量删除云存储内的图片   --- 云存储不支持 支付宝云 进行删除 所有采用云对象
		数据处理（图片缩放--缩略图）
	云函数API
10.用户体系 -- uni-id 用户体系 （重要开源库）-- schema表结构 - permission（CRUD）
	a.导入uni-id-pages并注册及登录
		common -- uni-config -- certer -- uni-id -- config  -- passwordStrength（密码强度）,tokenSecret和requestAuthSecret（生成token所用的密钥，加密token时的规则）
	b.tokenExpireshln保持客户端登录状态和unildRouter配置（类似拦截器）
	c.通过条件编译区别设置微信快捷登录和账号密码登录
	d.开通sms短信服务 login-by-sms短信验证码登录  --- 后台服务 -- 短信服务 -- 账户信息（充钱开通服务），模板配置（配置短信验证码）
	e.按需展示登录方式和用户服务隐私政策
    	f.借助内置openDB表创建并新增文章数据（新建表时选择别人做好的表结构；对于schema表的permission 权限设置）
	h.jql语句内云端环境变量（$cloudEnv_uid 用户uid，依赖uni-id）

