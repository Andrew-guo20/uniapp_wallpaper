# uniCloudWallPaper/ — 后端开发计划

> 关联总计划：[../../开发计划.md](../../开发计划.md) | PRD：[../../PRD.md](../../PRD.md)

## 当前进度

| 已完成 | 进行中 | 待开始 |
|--------|--------|--------|
| 6 表 + 32 方法（MVP） | — | uni-id 配置、Schema 迁移、模块化拆分、新增 Schema、新增方法、uni-admin |

---

## 阶段一：uni-id 用户体系（2 天）

### 1.1 配置 uni-id（0.5 天）
- [ ] 创建 `uni_modules/uni-config-center/uni-id/config.json`
- [ ] 配置微信小程序 OAuth（AppID + AppSecret）
- [ ] 配置 token 有效期（2h）+ 刷新阈值
- [ ] 上传部署 uni-id-common 公共模块
- [ ] 验证 uni-id 服务可用

### 1.2 Schema 字段迁移（0.25 天）
- [ ] `wallpaper-user-score` 新增 `uid` 字段
- [ ] `wallpaper-download-record` 新增 `uid` 字段
- [ ] 索引保留 `deviceId`，不强制唯一（迁移期并存）
- [ ] `wallpaper-list` 新增 `favoriteCount` 字段

### 1.3 云对象 `_before` 改造（0.25 天）
- [ ] 提取 `clientInfo.uid`（uni-id 登录后可用）
- [ ] `deviceId` 保留做 fallback
- [ ] 评分/下载写入同时写 `uid` + `deviceId`

### 1.4 用户查询兼容（0.25 天）
- [ ] `getUserInfo`：uid 优先查询，fallback deviceId
- [ ] `getUserWallList`：OR 查询覆盖迁移期数据
- [ ] `setupScore` / `downloadWall`：新数据双写

### 1.5 数据迁移脚本（0.5 天）
- [ ] 云对象新增 `migrateDeviceToUid()` 方法
- [ ] 用户首次登录 → 查找 deviceId 历史记录 → 写入 uid
- [ ] 幂等设计：可重复执行
- [ ] 迁移前数据备份

### 1.6 Schema 权限升级（0.25 天）
- [ ] `user-score`：create 条件 `auth.uid != null`
- [ ] `download-record`：create 条件 `auth.uid != null`
- [ ] 新表 `favorites`：delete 条件 `doc.uid == auth.uid`

---

## 阶段二：uni-admin 后台管理（3 天）

### 2.1 创建 uni-admin 项目（0.5 天）
- [ ] HBuilderX 新建 uni-admin 项目 → `admin/` 目录
- [ ] 关联同一 uniCloud 服务空间
- [ ] 注册 6 张现有表 Schema → 自动生成 CRUD 页面
- [ ] 配置管理员账号
- [ ] 验证基础 CRUD 可用

### 2.2 壁纸管理模块（0.5 天）
- [ ] 列表页：表格（缩略图、分类、评分、下载数、状态）
- [ ] 筛选：按分类 / 状态 / 时间范围
- [ ] 批量审核：勾选 → 一键通过 / 拒绝
- [ ] 编辑：分类、描述、标签
- [ ] 删除：级联（评分 + 收藏 + 评论 + 下载记录 + 云存储）
- [ ] 批量导入：JSON / CSV

### 2.3 分类管理模块（0.25 天）
- [ ] 列表 + 拖拽排序
- [ ] 新增/编辑（名称 + 封面图上传）
- [ ] 删除安全检查（关联壁纸数）

### 2.4 轮播图管理模块（0.25 天）
- [ ] 列表 + 排序
- [ ] CRUD + 图片裁剪上传
- [ ] 跳转配置（page / miniProgram）

### 2.5 公告管理模块（0.25 天）
- [ ] 列表 + 富文本编辑器
- [ ] 置顶/取消置顶
- [ ] 定时发布预留

### 2.6 用户管理模块（0.25 天）
- [ ] 用户列表（微信昵称/头像/注册时间/活跃数据）
- [ ] 用户详情（收藏数/评论数/下载数）

### 2.7 评论管理模块（0.25 天）
- [ ] 评论列表（按壁纸/用户/状态筛选）
- [ ] 审核通过/拒绝
- [ ] 批量删除

### 2.8 数据仪表盘（0.5 天）
- [ ] 概览卡片：总用户数 / 总壁纸数 / 今日下载量 / 待审核数
- [ ] 趋势图：近 7/30 天下载趋势
- [ ] 热门壁纸 TOP 10
- [ ] 分类壁纸分布饼图

### 2.9 投稿审核模块（0.25 天）
- [ ] 待审核列表
- [ ] 审核：通过 → 自动发布到 wallpaper-list / 拒绝 → 填写原因
- [ ] 已审核记录

---

## 阶段三：功能增强（3.5 天）

### 3.1 新增 Schema（0.25 天）
- [ ] `wallpaper-favorites.schema.json` — 收藏表
- [ ] `wallpaper-comments.schema.json` — 评论表
- [ ] `wallpaper-user-upload.schema.json` — 用户投稿表
- [ ] 上传部署 3 个 Schema

### 3.2 模块化拆分（1 天）
- [ ] 创建 `common.js` — db/dbCmd/mergeData/分页/敏感词
- [ ] 拆分 `wallpaper.js` — 前端 12 方法
- [ ] 拆分 `user.js` — 用户统计/历史
- [ ] 拆分 `admin-banner.js` — 轮播图 CRUD
- [ ] 拆分 `admin-classify.js` — 分类 CRUD
- [ ] 拆分 `admin-wallpaper.js` — 壁纸管理
- [ ] 拆分 `admin-news.js` — 公告 CRUD
- [ ] 重写 `index.obj.js` — 导入合并所有模块
- [ ] 部署验证 32 个原有方法正常工作

### 3.3 收藏方法（0.5 天）
- [ ] `addFavorite({wallId})` — 唯一索引防重复
- [ ] `removeFavorite({wallId})` — 同时更新 favoriteCount
- [ ] `toggleFavorite({wallId})` — 智能切换
- [ ] `isFavorited({wallIds})` — 批量查询
- [ ] `getUserWallList` 支持 `type=favorite`
- [ ] `getUserInfo` 返回 `favoriteSize`

### 3.4 评论方法（0.5 天）
- [ ] `addComment({wallId, content})` — 敏感词过滤 + 频率限制
- [ ] `getComments({wallId, pageNum, pageSize})` — 分页 + 仅返回 status=1
- [ ] `deleteComment({commentId})` — 只能删除自己的

### 3.5 推荐方法（0.5 天）
- [ ] `getRecommendWalls({pageSize=9})` — 基于用户行为的标签权重推荐
- [ ] 冷启动兜底：无历史数据 → 高评分随机
- [ ] 排除用户已浏览/下载的壁纸

### 3.6 投稿方法（0.5 天）
- [ ] `submitUpload({classid, description, tabs, picurl, smallPicurl})`
- [ ] `getMyUploads({pageNum, pageSize})` — 我的投稿 + 审核状态

### 3.7 搜索增强方法（0.25 天）
- [ ] `getHotSearchKeywords()` — 最近 7 天 TOP 10
- [ ] `getSearchHistory()` — 用户搜索历史（云端存储）
- [ ] `clearSearchHistory()` — 清除历史

---

## 阶段四：运营能力（2 天）

### 4.1 仪表盘 & 统计方法（0.5 天）
- [ ] `adminGetDashboardStats()` — 概览数据
- [ ] `adminGetDownloadTrend({days})` — 下载趋势
- [ ] `adminGetHotWalls({limit})` — 热门壁纸 TOP N

### 4.2 评论管理方法（0.25 天）
- [ ] `adminGetComments({pageNum, pageSize, status, wallId})`
- [ ] `adminReviewComment({_id, status})` — 审核
- [ ] `adminDeleteComment({_id})` — 管理员删除

### 4.3 投稿审核方法（0.25 天）
- [ ] `adminGetUploads({pageNum, pageSize, status})`
- [ ] `adminReviewUpload({_id, status, review_msg})` — 通过时自动发布到 wallpaper-list

### 4.4 定时触发器（0.5 天）
- [ ] 新建云函数 `wallpaper-task` 或利用现有 `_timing`
- [ ] 每周壁纸自动同步任务（调用爬虫 API）
- [ ] 每小时热门搜索词刷新
- [ ] 每天数据快照（用户数/壁纸数/下载量）

### 4.5 uni-push 推送集成（0.5 天）
- [ ] 封装 `sendPush({uid, title, content, payload})` 方法
- [ ] 投稿审核结果推送
- [ ] 新壁纸上架推送
- [ ] 系统公告推送

---

## 阶段五：测试与发布（1 天）

### 5.1 云对象全量回归测试
- [ ] 32 个原有方法功能正常
- [ ] 15 个新增方法功能正常
- [ ] 模块化拆分后性能无退化

### 5.2 数据库验证
- [ ] 3 张新表数据正确写入/查询
- [ ] deviceId → uid 迁移正确
- [ ] 级联删除正确

### 5.3 部署
- [ ] 上传部署所有云对象
- [ ] 上传部署所有 Schema
- [ ] uni-admin 项目部署

---

## 工时汇总（uniCloudWallPaper/ 板块）

| 阶段 | 内容 | 工时 |
|------|------|------|
| 阶段一 | uni-id 配置 + Schema 迁移 + 云对象改造 + 迁移脚本 | 2 天 |
| 阶段二 | uni-admin 搭建 + 8 个管理模块 | 3 天 |
| 阶段三 | 新增 Schema + 模块化拆分 + 收藏/评论/推荐/投稿/搜索方法 | 3.5 天 |
| 阶段四 | 统计/推送/定时触发器 | 2 天 |
| 阶段五 | 测试 + 部署 | 1 天 |
| **合计** | | **11.5 天** |
